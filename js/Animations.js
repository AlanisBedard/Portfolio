export default class Animations {
  constructor(element) {
    this.init();
  }

  init() {
    const swup = new Swup(); // only this line when included with script tag
    console.log("swup");
  }
}
